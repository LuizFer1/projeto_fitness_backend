## Codebase Patterns
- PHP binary is at /tmp/php — use `export PATH="/tmp:$PATH"` before running php/artisan commands
- Route model binding for username is in routes/api.php via Route::bind('username', ...) — NOT via getRouteKeyName on User model
- User model uses UUID as primary key (HasUuids trait), foreign keys are `user_uuid`
- FriendshipRepository has statusBetween(), areFriends(), isBlocked() — use for all friendship checks
- PublicProfileResource uses fluent ->withFriendshipStatus() to inject friendship status
- UseCases live in app/Application/UseCases/{Domain}/
- Resources are namespaced by domain: app/Http/Resources/PublicProfile/, app/Http/Resources/Gamification/
- LevelCalculator::nameFor($level) returns Portuguese level names (Iniciante, Dedicado, etc.)
- All API error responses must use shape: { "error": { "code": "...", "message": "...", "details": {} } }
- Tests use RefreshDatabase trait; factories exist for User, Goal, Achievement, UserAchievement, Friendship, XpTransaction
- Friendship model needs HasFactory trait for FriendshipFactory to work
- throttle:api rate limiter is NOT defined — don't use it in route middleware
- For 404 with custom JSON shape, use response()->json([...], 404) not abort(404)
- Route parameter {username} resolves to User via Route::bind in api.php — controller receives User $username

## 2026-04-06 - US-001
- Verified existing implementation from commit 8ce08e2
- Route binding by username with is_active filter in routes/api.php
- PublicProfileResource exposes only: username, name, avatar, bio, level, level_name, xp_points, achievements_count, goals_completed_count, friendship_status
- PII fields (email, cpf, birth_date, weight_kg, height_cm, gender, health_profile, etc.) are NOT exposed
- 5 unit tests in tests/Unit/Resources/PublicProfileResourceTest.php — all passing
- All 132 project tests pass
- **Learnings for future iterations:**
  - User model has SoftDeletes — soft-deleted users are automatically excluded from queries
  - Route::bind('username') uses firstOrFail() which throws ModelNotFoundException → 404
  - achievements_count and goals_completed_count are set via loadCount/withCount, not stored columns
---

## 2026-04-06 - US-002
- Implemented GetPublicProfileUseCase with block checking via FriendshipRepository
- Created PublicProfileController with show() method returning PublicProfileResource
- Added route GET /api/v1/users/{username} under auth:sanctum
- Created FriendshipFactory with blocked() and pending() states
- Added HasFactory trait to Friendship model
- 9 feature tests: active profile, inactive 404, nonexistent 404, blocked both directions 404, PII hidden, soft-deleted 404, unauthenticated 401, friendship status
- Files: GetPublicProfileUseCase.php, PublicProfileController.php, FriendshipFactory.php, Friendship.php, api.php, ShowProfileTest.php
- **Learnings for future iterations:**
  - throttle:api rate limiter is not configured — caused 500 errors in tests
  - Route::bind('username') resolves to User model — controller param type is User $username
  - No Goals table/model exists yet — goals_completed_count hardcoded to 0
  - Use response()->json() for custom 404 shape instead of abort()
---

## 2026-04-06 - US-003
- Implemented GET /api/v1/users/{username}/achievements endpoint
- Created GetPublicAchievementsUseCase with block checking, paginated 30/page, ordered by unlocked_at desc
- Created PublicAchievementResource (without is_unlocked field — all are unlocked by definition)
- Created AchievementFactory and UserAchievementFactory; added HasFactory to UserAchievement model
- Added achievements() method to PublicProfileController
- 8 feature tests: unlocked list, ordering, both block directions 404, inactive 404, nonexistent 404, unauthenticated 401, empty results
- All 149 project tests pass
- **Learnings for future iterations:**
  - AchievementFactory and UserAchievementFactory now exist for test data
  - UserAchievement model now has HasFactory trait
  - User->achievements() returns UserAchievement (not Achievement) — eager load ->with('achievement') to get achievement data
---
